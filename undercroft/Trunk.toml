# `trunk build` (dev) or `trunk build --release` from this directory.
# Output goes to dist/, which the tailnet static server exposes as /undercroft/dist/.
[build]
target = "web/index.html"
dist = "dist"
public_url = "./"
filehash = false

[serve]
addresses = ["0.0.0.0"]
port = 8766
